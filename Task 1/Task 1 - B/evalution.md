file 1 numpy_task:
    1. include how to to convert normal python list to an numpy array 
    2. we can traverse through the np array like a normal iterable object abd update element by element in the np.array()
    3. undersatnd ablout matrix multiplication and how does the shape 
    4. utlization of simple vectorized computation 

file 2 panda_task:
    1. loading and csv dataset
    2. handling with  absent values by substituting it with the mean of the feature(column)
    3. representing the mean median stanard divation of the data 

ANSWERS:
1. 
supervised learning as a method of train the the machine learning model in with model is provided to the machine leaning model alcong with the correct answer so that the model can understand and learn 

2. 
regression the the model which used the data on which it is train and then a set of data is given to the model using which it predict the representation, eg predict the run of the t20 match based on current condition 

meanwhile classification does a simipilar thing but instead the data is a probibilty of classifaction, eg will the person suffer through an heart problem or who will win a t20 match 

3.
loss functionm is a equation which we use to understand how much difference is between the predicted the value and actual value. it helps us understand the flaw in our model eg mean squared error

4.
gradeint decesent is an algorithm what tries to minimzed the loss function by learning from mistaske and editing the weights of each neurons 

5.
numpy helps us to easily handle laarge array ir list of data and operate  on them much faster then compared to regular python lists and allow ud to ustile multi dimensional arrays.
pandas helps us to handle large datasets and make it easier and faster to manipulate, clean and understand data present in the datasets.